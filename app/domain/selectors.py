"""Logical selector-to-locator mapping loader (placeholder)."""

from __future__ import annotations

from typing import Dict


def load_selectors(profile: str = "default") -> Dict[str, str]:
    """Load selector mappings. Placeholder returns an empty mapping."""
    return {}
