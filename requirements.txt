# 네이버 스마트플레이스 리뷰 자동응답 시스템 필수 의존성

# GUI 프레임워크
PySide6>=6.5.0

# 웹 자동화 (향후 기능 구현용)
playwright>=1.40.0

# HTTP 클라이언트
requests>=2.31.0

# 데이터베이스
# sqlite3 는 Python 내장 모듈이므로 별도 설치 불필요

# LLM API 클라이언트
openai>=1.0.0

# 설정 파일 처리
PyYAML>=6.0

# 로깅 및 유틸리티
python-dateutil>=2.8.0

# 개발/테스트용 (선택적)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
watchdog>=6.0.0  # 개발 모드 자동 재시작
